const numbers = [10, 20, 30, 40, 50];

const firstNum = numbers[0];
const secondNum = numbers[1];

firstNum;
secondNum;

const [first, second, ...rest] = numbers;

first;
second;
rest;

// BOTH RETURN SAME VALUE fisrt-firstnum
// REST RETURN REST OF THE ARRAY
